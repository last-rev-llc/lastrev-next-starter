import React from 'react';
import { render } from '@testing-library/react';
import TopNavBar from './TopNavBar';
import mockContent from './TopNavBar.mock';

describe('<TopNavBar />', () => {
  describe('<TopNavBar /> renders correctly', () => {
    test.todo('write tests');
  });
});
