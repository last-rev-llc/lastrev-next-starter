import React from 'react';
import { render } from '@testing-library/react';
import TopNavItem from './TopNavItem';
import mockContent from './TopNavItem.mock';

describe('<TopNavItem />', () => {
  describe('<TopNavItem /> renders correctly', () => {
    test.todo('write tests');
  });
});
