import { cleanup } from '@testing-library/react';

afterEach(() => {
  cleanup();
});

describe('<Asset />', () => {
  describe('<Asset /> renders correctly', () => {
    test.todo('write tests');
  });
});
